beautifulsoup4
lxml
requests

# preprocessing (PDF bytes -> text)
pymupdf

# spaCy extraction
spacy>=3.8,<3.9
# NOTE: Relation extraction is implemented as a custom TrainablePipe in
# `extraction_spacy/relation_extractor.py` (no spacy-experimental dependency).

